Extract ONLY subcategories that you can ACTUALLY SEE in this document image.
DO NOT make up or hallucinate categories.

IMPORTANT: Maintain consistent formatting for all subcategories.
Use hyphens (-) as separators between category types (e.g., "Firearm Accessories - Grips").
Follow the exact format shown in the image.

Return ONLY a valid JSON with a "cypher_query" field.

{
  "cypher_query": "MERGE..."
}